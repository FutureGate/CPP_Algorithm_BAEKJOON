#include <stdio.h>

int power2(int n) {
	int result = 1;
	
	while(n--) {
		result *= 2;
	}
	
	return result;
}

int main(void) {
	int i = 0, j = 1, value = 0;
	int n, r, c;
	
	scanf("%d %d %d", &n, &r, &c);
	
	while(1) {
		if( <= power2(i)) {
			
		}
	}
	
}
