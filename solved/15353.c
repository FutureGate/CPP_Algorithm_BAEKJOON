#include <stdio.h>
#include <string.h>

int main(void) {
	int pivot = 0;
	char a[10002];
	char b[10002];
	
	int i, length;
	
	scanf("%s %s", a, b);
	
	(strlen(a) >= strlen(b))? length = strlen(b);
	
	for(i=0; 
}
